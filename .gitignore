# batch job logs / output areas (regenerated by the scripts)
Logs/
LogsHI/
work*/

# job packaging + CMSSW/ROOT build & data artifacts, in case any slip in later
*.tgz
*.root
*.so
*.d
CMSSW_*/
.SCRAM/
tmp/

# editor / OS junk
*.swp
.DS_Store

# never commit credentials
*.pem
*.key
x509up*
krb5cc*
